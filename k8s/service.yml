apiVersion: v1
kind: Service

metadata:
  name: devops-api-service
  namespace: devops-lab

spec:
  type: NodePort

  selector:
    app: devops-api

  ports:
    - name: http
      port: 80
      targetPort: 8080